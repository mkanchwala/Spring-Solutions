# JellyBelly Base Server Details
timeoutInMS=${timeoutInMS}
baseUrl=${baseUrl}
cacheExpirationTime=${cacheExpirationTime}

#Database Configuration
db.driver=${db.driver}
db.url=${db.url}
db.username=${db.username}
db.password=${db.password}

#Hibernate Configuration
hibernate.dialect=${hibernate.dialect}
hibernate.format_sql=${hibernate.format_sql}
hibernate.hbm2ddl.auto=${hibernate.hbm2ddl.auto}
hibernate.ejb.naming_strategy=${hibernate.ejb.naming_strategy}
hibernate.show_sql=${hibernate.show_sql}

#Email and server configuration
resource.loader=${resource.loader}
file.resource.loader.cache=${file.resource.loader.cache}
file.resource.loader.class=${file.resource.loader.class}
file.resource.loader.path=${file.resource.loader.path}
smtpHost=${smtpHost}
smtpPort=${smtpPort}
authenticatorEmail=${authenticatorEmail}
authenticatorPassword=${authenticatorPassword}
fromName=${fromName}
templateBasePath=${temaplateBasePath}
bccAddress=${bccAddress}
fromAddress=${fromAddress}
subject=${subject}

#Facebook
facebook.app.id=${facebook.app.id}
facebook.app.secret=${facebook.app.secret}

#Twitter
twitter.consumer.key=${twitter.consumer.key}
twitter.consumer.secret=${twitter.consumer.secret}
